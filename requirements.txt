# PyTorch and related libraries
# Updated for MPS autocast support (requires PyTorch 2.1+)
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Audio processing libraries
soundfile>=0.12.0
librosa>=0.9.0

# Montreal Forced Aligner for phoneme duration extraction
# Install via conda: conda install -c conda-forge montreal-forced-aligner
# Or pip: pip install montreal-forced-aligner
montreal-forced-aligner>=3.0.0
tgt>=1.4.4  # TextGrid tools for parsing MFA output

# Data processing and utilities
numpy>=1.21.0
pathlib

# Progress bars and logging
tqdm>=4.64.0

# TensorBoard and its dependencies
tensorboard==2.20.0
tensorboard-data-server<0.8.0,>=0.7.0
torch_tb_profiler==0.4.3
markdown>=2.6.8
protobuf!=4.24.0,>=3.19.6
grpcio>=1.48.2
werkzeug>=1.0.1
pillow
absl-py>=0.4

# Standard library dependencies (usually included with Python)
# os
# json
# logging
# re
# typing
# dataclasses

# Optional: For better performance (if using CUDA)
# torch-audio  # Alternative audio processing

# Development and debugging (optional)
matplotlib>=3.5.0  # For visualization during development
